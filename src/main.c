#include <stdio.h>

#include "recursiveFib.h"
#include "dynamicFib.h"

/**
 * @brief This is the main function remove the // befor the function you want to test.
 * 
 * @return int 
 */
int main(void) // int argc, char const *argv[]
{
    // RecursiveFib(10);   //Works fine for smaller numbers -> Really slow with higher numbers >= 40
    // DynamicFib(60);     //Really fast also works with higher numbers

    return 0;
}
